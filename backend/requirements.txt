# Core dependencies — required to run the API
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0
httpx==0.27.0
aiohttp==3.10.0
sse-starlette==2.1.0
python-multipart==0.0.9
structlog==24.2.0
diskcache==5.6.3
pyyaml==6.0.1
python-dotenv==1.0.1
beautifulsoup4==4.12.3

# LLM & agent framework
langchain==0.3.0
langchain-community==0.3.0
langchain-groq==0.2.0

# Web search
duckduckgo-search==6.3.0

# Phase 1 — Real review scraping + DistilBERT sentiment
playwright==1.48.0
lxml==5.3.0
transformers==4.45.0
# torch is a heavy dep; install the CPU wheel:
#   pip install torch==2.4.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.4.0

# Phase 4 — Observability
langfuse==2.50.0

# Phase 9 — Semantic business search
sentence-transformers==3.1.0
chromadb==0.5.0

# Phase 10 — Voice input
# Whisper runs on a remote server; this backend forwards audio over HTTP.

# Testing
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==5.0.0
