"use client";

interface PromptToolBarProps {}

export default function PromptToolBar({}: PromptToolBarProps) {
  return (
    <div className="relative">
      {/* here add a button to add a new chat */}
      {/* here add a toggle button to switch between chat and search */}
      {/* Here add a drop select options with grouped for choosing LLM models like Meta{llamma3:latest,gemma3:12b,gemma4}.grok{grok-4.3 or which are free to use}` */}
    </div>
  );
}
